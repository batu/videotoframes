"""Scope-local conftest for the find_the_dog validation suite.

The `--run-validation` flag is declared at the top-level
tests/conftest.py so pytest collects it early enough; this file is a
placeholder to document that the suite lives here and to house any
future fixtures that shouldn't leak to unit tests.
"""
